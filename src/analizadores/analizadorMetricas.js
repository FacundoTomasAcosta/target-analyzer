// Acá tenemos que programar la función asincrona obtenerMetricas y exportarla.

export async function obtenerMetricas(page) {
  // Programar la función la cual extrae las métricas con puppeteer, los documentos cargados, la cantidad de frames activos, los nodos (elementos del DOM),
  // eventos registrados de JavaScript, el layoutCount (cantidad de veces que Chromium tuvo que recalcular la disposición visual de la página), y el recalcStyleCount
  // cuántas veces Chormium recalculó los estilos de CSS.
}
